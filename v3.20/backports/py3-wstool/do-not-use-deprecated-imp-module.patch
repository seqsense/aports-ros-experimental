diff --git a/setup.py b/setup.py
index 51a1c0b..fba4444 100644
--- a/setup.py
+++ b/setup.py
@@ -4,20 +4,14 @@ from distutils.command.build_py import build_py
 
 import os
 import sys
-import imp
 import argparse
 
+sys.path.append("src/wstool")
+from __version__ import version
+
 
 def get_version():
-    ver_file = None
-    try:
-        ver_file, pathname, description = imp.find_module('__version__', ['src/wstool'])
-        vermod = imp.load_module('__version__', ver_file, pathname, description)
-        version = vermod.version
-        return version
-    finally:
-        if ver_file is not None:
-            ver_file.close()
+    return version
 
 
 def _resolve_prefix(prefix, type):
